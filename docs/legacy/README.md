# Legacy Reference Surfaces

These directories are archived reference code, not active product surfaces.

- `chrome-extension-reference/` contains the old Chrome extension cockpit.
- `react-frontend-reference/` contains the old standalone React dashboard.

The v1 product path is the Electron desktop app. New product work should go
through `desktop/`, `dashboard/`, and the Python helper modules.
